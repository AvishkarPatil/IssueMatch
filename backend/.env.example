# Copy this file to backend/.env and fill values before running the backend
# cp backend/.env.example backend/.env

# ---------- GitHub OAuth ----------
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# ---------- Database ----------
# Example Postgres URL: postgresql://user:password@localhost:5432/issuematch_db
DATABASE_URL=postgresql://user:password@localhost:5432/issuematch_db

# ---------- App settings ----------
# Use a long random value for SECRET_KEY in production
SECRET_KEY=replace-with-a-random-secret
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# ---------- Optional: AI / cache / monitoring ----------
OPENAI_API_KEY=             # optional: if using OpenAI for embeddings
REDIS_URL=redis://localhost:6379/0
SENTRY_DSN=                 # optional: add if using Sentry

# NOTE: Do NOT commit real secrets. This file is a template only.
